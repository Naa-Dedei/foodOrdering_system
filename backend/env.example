# Render Postgres "External Database URL"
# Example format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=

# Server port (default is 3000 if PORT is not set)
# PORT=3000

# Set to "false" only if your DB does not require SSL.
# Render Postgres typically requires SSL, so leave this unset.
# DATABASE_SSL=false


